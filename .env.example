# Contoh isi .env — salin file ini menjadi `.env` lalu isi nilainya.
# `.env` sengaja tidak di-commit; file contoh ini yang masuk repo.

BASE_URL=https://api.example.com
API_KEY=isi-dengan-api-key-dari-tim-backend
